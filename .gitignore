node_modules/
dist/
dist-worker
build/
coverage/
.env
.env.*
!.env.example

# tooling caches
*.tsbuildinfo
.eslintcache
.nest/

# runtime / local data
logs/
*.log
npm-debug.log*
tmp/
uploads/
# The local file-storage driver's default directory (DEFAULT_LOCAL_STORAGE_DIR). It holds deposit proof
# images, which are players' financial documents, so it must never be committed; test runs write here too.
.storage/

# editors / OS
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
